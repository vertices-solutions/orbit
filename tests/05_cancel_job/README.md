# 05_cancel_job

Long-running job that should be canceled via `orbit job cancel`.

Suggested commands:
- orbit job submit --to <cluster> tests/05_cancel_job
- orbit job cancel <job_id>
- orbit job get <job_id>
