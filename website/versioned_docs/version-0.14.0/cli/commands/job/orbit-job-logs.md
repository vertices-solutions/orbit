---
title: "orbit job logs"
description: "Show job logs"
---

Show job logs

Parent command: [`orbit job`](./orbit-job)

**Usage:** `orbit job logs [OPTIONS] <JOB_ID>`

## Arguments

- `<JOB_ID>`: Job id from the daemon

## Flags and Options

- `--err`: Show stderr instead of stdout
- `--non-interactive`: Run without prompts, fail on MFA, and output JSON only.
- `-h, --help`: Print help