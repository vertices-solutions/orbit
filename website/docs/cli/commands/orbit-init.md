---
title: "orbit init"
description: "Initialize a project directory with an Orbitfile"
---

Initialize a project directory with an Orbitfile

Parent command: [`orbit`](./orbit)

**Usage:** `orbit init [OPTIONS] <PATH>`

## Arguments

- `<PATH>`

## Flags and Options

- `--name <NAME>`: Blueprint name stored in Orbitfile and used when building blueprints
- `--non-interactive`: Run without prompts, fail on MFA, and output JSON only.
- `-h, --help`: Print help