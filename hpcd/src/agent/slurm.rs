use serde::{Deserialize, Serialize};

#[derive(Debug, Serialize, Deserialize)]
pub struct SlurmNode {
    pub architecture: String,
    pub burstbuffer_network_address: String,
    pub boards: u32,
    pub boot_time: u64,
    pub comment: String,
    pub cores: u32,
    pub cpu_binding: u32,
    pub cpu_load: u32,
    pub extra: String,
    pub free_memory: u32,
    pub cpus: u32,
    pub last_busy: u64,
    pub features: String,
    pub active_features: String,
    pub gres: String,
    pub gres_drained: String,
    pub gres_used: String,
    pub mcs_label: String,
    pub name: String,
    pub next_state_after_reboot: String,
    pub address: String,
    pub hostname: String,
    pub state: String,
    pub state_flags: Vec<String>,
    pub next_state_after_reboot_flags: Vec<String>,
    pub operating_system: String,
    pub owner: Option<String>,
    pub partitions: Vec<String>,
    pub port: u16,
    pub real_memory: u32,
    pub reason: String,
    pub reason_changed_at: u64,
    pub reason_set_by_user: Option<String>,
    pub slurmd_start_time: u64,
    pub sockets: u32,
    pub threads: u32,
    pub temporary_disk: u64,
    pub weight: u32,
    pub tres: String,
    pub slurmd_version: String,
    pub alloc_memory: u32,
    pub alloc_cpus: u32,
    pub idle_cpus: u32,
    pub tres_used: Option<String>,
    pub tres_weighted: f64,
}
